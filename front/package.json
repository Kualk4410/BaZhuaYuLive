{
  "name": "ai-live-app-v2",
  "version": "1.0.0",
  "private": true,
  "description": "AI Live App v2",
  "main": "electron.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix renderer",
    "dev": "concurrently \"cd .. && python back/run.py\" \"npm --prefix renderer run dev\" \"wait-on http://localhost:5173 && .\\node_modules\\.bin\\electron .\""
 },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^28.1.0",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "keytar": "^7.9.0"
  }
}